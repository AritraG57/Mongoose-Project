<header>
    <nav class="fixed top-0 left-0 bg-red-500 h-12 w-full flex items-center px-6">
      <div class="text-white font-semibold text-lg mr-8">
        Airbnb
      </div>

      <ul class="flex space-x-6 text-white text-sm">
        <li>
          <a href="/" class="<%= currentPage === 'index' ? 'bg-red-400' : 'hover:border-white' %> border border-transparent   p-1.5 rounded-sm">Home</a>
        </li>
        <li>
          <a href="/add-home" class="<%= currentPage === 'add-home' ? 'bg-red-400' : 'hover:border-white' %> border border-transparent   p-1.5 rounded-sm
">Add Home</a>
        </li>
        <li>
          <a href="/favourites" class="<%= currentPage === 'favourites' ? 'bg-red-400' : 'hover:border-white' %> border border-transparent   p-1.5 rounded-sm">Favourites</a>
        </li>
        

        <li>
          <a href="/host/host-homes" class="<%= currentPage === 'host-homes' ? 'bg-red-400' : 'hover:border-white' %> border border-transparent   p-1.5 rounded-sm">Host Homes</a>
        </li>
      </ul>
    </nav>
  </header>