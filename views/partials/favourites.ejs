<form action="/favourites" method="POST">
  <button
    type="submit"
    class="w-full p-2 bg-red-500 hover:bg-red-600 text-white font-semibold py-3 rounded-lg transition"
  >
    Add to Favourites
  </button>
  <input type="hidden" name="id" value="<%= home._id %>" />
</form>
